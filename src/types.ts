export interface Train {
    train_id: string;
    train_name: string;
    // number: string;
    departure_time: string;
    // arrival: string;
    // duration: string;
    price: number;
    // seatsAvailable: number;
}
